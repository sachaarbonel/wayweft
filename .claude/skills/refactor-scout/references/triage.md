# Triage

- Safe quick wins: findings with `fix.safe === true`
- Architectural hotspots: `architecture` findings with score >= 70
- Human review required: low-confidence findings or public API changes
